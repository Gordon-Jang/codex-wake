interface:
  display_name: "Wake"
  short_description: "Checkpoint-first continuation for long Codex jobs across quota windows."
  default_prompt: "Use $wake to persist the current task as a compact checkpoint-backed job. On quota recovery, continue in a new lightweight thread from the checkpoint instead of resuming the old chat."

policy:
  allow_implicit_invocation: true
